[package]
name = "xpui-simulator"
description = "Runs an xpui app in a desktop window, for developing screens without hardware"
readme = "README.md"
keywords = ["ui", "embedded", "eink", "simulator", "xpui"]
categories = ["development-tools", "gui"]
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[lib]
name = "xpui_simulator"
crate-type = ["rlib"]

[dependencies]
xpui.workspace = true
xpui-boards.workspace = true
xpui-chrome.workspace = true
# `framebuffer` for the screenshot key: it is the BMP writer the screenshot
# tests already use, and a second one here would be a second thing to get
# wrong. Host-only, which this crate is anyway — it links SDL.
xpui-embedded-graphics = { workspace = true, features = ["framebuffer"] }
embedded-graphics = "0.8"
embedded-graphics-simulator = "0.8"

[dev-dependencies]
# The guide shows how to screenshot a screen without a window, and that block
# is compiled rather than taken on trust.
xpui = { workspace = true, features = ["testing"] }
